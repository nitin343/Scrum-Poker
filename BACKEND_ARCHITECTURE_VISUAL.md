# 🏗️ Backend Architecture Visual Guide

## Project Structure Overview

```
scrum-poker/
├── server/                          # Backend Node.js/Express server
│   ├── src/
│   │   ├── api/v1/                  # 🆕 API Versioning Layer
│   │   │   ├── routes/
│   │   │   │   ├── auth.ts          # Authentication endpoints
│   │   │   │   ├── jira.ts          # Jira integration endpoints
│   │   │   │   └── index.ts         # Route aggregator
│   │   │   └── middleware/
│   │   │       └── auth.ts          # JWT authentication
│   │   │
│   │   ├── services/                # 🆕 Business Logic Layer
│   │   │   ├── JiraService.ts       # Jira operations
│   │   │   ├── AuthService.ts       # Auth operations
│   │   │   ├── SprintService.ts     # Sprint management
│   │   │   ├── GameService.ts       # Game/voting logic
│   │   │   └── index.ts             # Service exports
│   │   │
│   │   ├── models/                  # Database Models
│   │   │   ├── Admin.ts
│   │   │   ├── Company.ts
│   │   │   └── Sprint.ts
│   │   │
│   │   ├── middleware/              # Shared Middleware
│   │   │   ├── auth.ts
│   │   │   └── errorMiddleware.ts
│   │   │
│   │   ├── utils/                   # Utilities & Helpers
│   │   │   ├── logger/
│   │   │   │   └── Logger.ts
│   │   │   ├── errors/
│   │   │   │   ├── AppError.ts
│   │   │   │   └── ErrorHandler.ts
│   │   │   ├── validators/
│   │   │   └── constants/
│   │   │
│   │   ├── db/                      # Database
│   │   │   └── connection.ts
│   │   │
│   │   ├── types/                   # TypeScript Types
│   │   │   └── jira-client.d.ts
│   │   │
│   │   ├── dev-scripts/             # 🆕 Dev Utilities
│   │   │   ├── test-user-snippet.js
│   │   │   └── verify-jira.ts
│   │   │
│   │   ├── __tests__/               # Tests
│   │   │   ├── auth.test.ts
│   │   │   ├── jira.test.ts
│   │   │   └── setup.ts
│   │   │
│   │   ├── socket.ts                # WebSocket Setup
│   │   ├── store.ts                 # In-Memory Store
│   │   ├── app.ts                   # Express Config
│   │   └── index.ts                 # Server Entry Point
│   │
│   ├── ARCHITECTURE.md              # 🆕 Architecture Guide
│   ├── CLEANUP_SUMMARY.md           # 🆕 Cleanup Summary
│   └── package.json
│
└── client/                          # Frontend React/Vite
```

## 🔄 Request Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ CLIENT REQUEST                                              │
│ (Browser/HTTP Client)                                       │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ EXPRESS APP (app.ts)                                        │
│ - CORS Configuration                                        │
│ - JSON Parser                                               │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ ROUTE AGGREGATOR (/api/v1/routes/index.ts)                 │
│ ├─ /auth  → auth.ts routes                                 │
│ └─ /jira  → jira.ts routes                                 │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ MIDDLEWARE                                                  │
│ ├─ Error Middleware                                        │
│ └─ Auth Middleware (if protected route)                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ ROUTE HANDLER                                               │
│ (e.g., POST /api/v1/auth/login)                            │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ SERVICE LAYER                                               │
│ ├─ AuthService (signup, login, token)                      │
│ ├─ JiraService (client, connection, boards)                │
│ ├─ SprintService (CRUD, sync)                              │
│ └─ GameService (voting, game logic)                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ DATA MODELS                                                 │
│ ├─ Admin (MongoDB)                                         │
│ ├─ Company (MongoDB)                                       │
│ └─ Sprint (MongoDB)                                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ DATABASE                                                    │
│ (MongoDB)                                                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ RESPONSE                                                    │
│ (JSON Response to Client)                                  │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Service Interaction Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                    ROUTE HANDLERS                            │
│  (api/v1/routes/auth.ts, jira.ts, etc.)                     │
└──────────┬───────────────────────────────────────┬───────────┘
           │                                       │
           ▼                                       ▼
      ┌─────────────┐                       ┌──────────────┐
      │ AuthService │                       │ JiraService  │
      ├─────────────┤                       ├──────────────┤
      │ • generateToken()                   │ • initClient()
      │ • verifyToken()                     │ • testConnection()
      │ • signup()                          │ • getBoards()
      │ • login()                           │ • getSprints()
      │ • getAdminById()                    │ • getSprintIssues()
      └────────┬────────────────────────────┘
               │
      ┌────────┴──────────┐
      │                   │
      ▼                   ▼
┌──────────────┐  ┌──────────────────┐
│ Admin Model  │  │ SprintService    │
│ (MongoDB)    │  ├──────────────────┤
└──────────────┘  │ • getCompanySprints()
                  │ • createSprint()
                  │ • updateSprint()
                  │ • syncFromJira()
                  └────────┬─────────┘
                           │
              ┌────────────┴───────────┐
              │                        │
              ▼                        ▼
         ┌─────────┐          ┌─────────────┐
         │ Jira    │          │ GameService │
         │ Client  │          ├─────────────┤
         └─────────┘          │ • createGame()
                              │ • recordVote()
                              │ • endGame()
                              │ • getResults()
                              └─────────────┘
```

## 🔐 Authentication Flow

```
USER LOGIN REQUEST
       │
       ▼
┌──────────────────┐
│ POST /api/v1/    │
│   auth/login     │
└────────┬─────────┘
         │
         ▼
    ┌─────────────────────────┐
    │ Validate Credentials    │
    │ (AuthService.login())   │
    └────────┬────────────────┘
             │
             ▼
       ┌─────────────────────────────┐
       │ Find User in Database       │
       │ (Admin.findOne())           │
       └────────┬────────────────────┘
                │
                ▼
          ┌──────────────────┐
          │ Compare Password │
          │ (bcrypt)         │
          └────────┬─────────┘
                   │ Success
                   ▼
         ┌──────────────────────┐
         │ Generate JWT Token   │
         │ (AuthService         │
         │  .generateToken())   │
         └────────┬─────────────┘
                  │
                  ▼
         ┌────────────────────┐
         │ Return Token       │
         │ + User Data        │
         │ (to Client)        │
         └────────────────────┘
         
         
PROTECTED ROUTE REQUEST
       │
       ▼
┌────────────────────────────┐
│ GET /api/v1/auth/me        │
│ Headers: Authorization:    │
│  Bearer <token>            │
└────────┬───────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Auth Middleware             │
│ (/api/v1/middleware/auth.ts)│
└────────┬────────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Extract Token from       │
│ Authorization Header     │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Verify JWT               │
│ (AuthService            │
│  .verifyToken())        │
└────────┬─────────────────┘
         │ Valid
         ▼
┌──────────────────────┐
│ Attach User to       │
│ Request Object       │
│ (req.user)           │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ Route Handler        │
│ Proceeds             │
└──────────────────────┘
```

## 📈 Scaling Readiness

```
Current State:
┌─────────────┐
│ /api/v1/    │ ← Ready for v1 clients
│ /api/v2/    │ ← Easy to add when needed
│ /api/v3/    │ ← Future-proof
└─────────────┘

Service Reusability:
┌────────────────┐
│ AuthService    │ ← Used by:
│                │   • HTTP routes
│                │   • WebSocket handlers
│                │   • Future CLI tools
└────────────────┘

┌────────────────┐
│ JiraService    │ ← Used by:
│                │   • HTTP routes
│                │   • Scheduled jobs
│                │   • Background workers
└────────────────┘
```

## ✅ Code Organization Best Practices

```
✅ SINGLE RESPONSIBILITY PRINCIPLE
   Each service has ONE main concern
   - JiraService: Jira operations only
   - AuthService: Auth operations only
   - GameService: Game logic only

✅ SEPARATION OF CONCERNS
   Routes → Services → Models → DB
   Each layer has its purpose

✅ DRY (Don't Repeat Yourself)
   Services are reusable
   No duplicated business logic

✅ LOOSE COUPLING
   Services don't depend on routes
   Routes depend on services
   Easy to test and replace

✅ STRUCTURED LOGGING
   No console.log pollution
   Logger with levels and context
   Easy to trace and debug

✅ TYPE SAFETY
   Full TypeScript support
   Interfaces for all major objects
   Compiler catches errors early
```

## 🚀 Quick Reference

**What to modify for new features:**

1. **New Service** → Create file in `/services/`
2. **New Endpoint** → Add route in `/api/v1/routes/`
3. **New Validation** → Add validator in `/utils/validators/`
4. **New Data Model** → Create model in `/models/`
5. **New Constant** → Add to `/utils/constants/`
6. **New Type** → Add to `/types/`

---

**Architecture Decision: ✅ CLEAN, SCALABLE, MAINTAINABLE**
